# 必须与 MP 的 JWT_SECRET 完全一致
JWT_SECRET=change_me_to_a_long_random_secret

# Postgres（本模板独立库，与 MP 分离）
POSTGRES_PASSWORD=game_dev_pwd
POSTGRES_USER=game
POSTGRES_DB=game

# 本游戏的 game_id（clone 成 game-xxx 后改成自己的，并与 MP Catalog 中注册的一致）
GAME_ID=match3
